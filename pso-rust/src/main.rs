#![allow(non_snake_case)] //disabling damn snake case warnings

mod point;

fn main() 
{
    let EPOCH = 100;
    let POINTSAMOUNT = 2000;
    let BOUND: (f64, f64) = (-10.0, 10.0);
    let ALPHA = 0.5;
    let BETA = 0.3; 
    let POINTDIMENSION = 8; //set to 2 for func1, to 4 for func2, 8 fo func3
    let SAME_GRADE_EPOCHS = 10;

}
